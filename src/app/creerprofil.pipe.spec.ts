import { CreerprofilPipe } from './creerprofil.pipe';

describe('CreerprofilPipe', () => {
  it('create an instance', () => {
    const pipe = new CreerprofilPipe();
    expect(pipe).toBeTruthy();
  });
});
